# 稿定 · 用户体验报告（v2.13.1 复测版）

> 生成时间：2026/8/13
> 依据：以真人作者身份用真实浏览器（Chrome）逐区操作所得的实际体验记忆 —— 覆盖 对比区 / 大纲区 / 定稿区 / 素材台 / 反AI / 词库管理 / 设置·Provider / 设定 / 前文库 / AI 深度辅助 / 持久化。
> 本报告 = ① 历史修复跟踪（UX-01~16）+ ② 本次复测新观察 + ③ 建议添加的便利功能 + ④ 优化点优先级。
> 功能 Bug 见 `BUG_REPORT.md`（含本次新发现的 Ctrl+Shift+Enter 快捷键失效）。

---

## 一、历史体验不足（UX-01~16）修复跟踪

> 上一轮（v2.12 提出）的 16 条已在 v2.13.0 全部落地，本次 v2.13.1 复测确认**无回退**。

| 项 | 修复 | 本次复测 |
|---|---|---|
| UX-01 AI 面板与正文同屏 | `show-ai` 时分栏（正文 42% + AI 面板） | ✅ 复测通过 |
| UX-02 补全候选持续显示 | `#btn-suggest` mousedown 防夺焦 | ✅ 弹窗全程显示可点选 |
| UX-03 流式失败可诊断 | 描述性报错 + 空产出写日志 | ✅ 已修复 |
| UX-04 清空/重选确认 | 两处 `confirm` | ✅ 复测通过 |
| UX-05 栏数重排提示 | 变化时 toast | ✅ |
| UX-06 批量保存进度 | 「保存中…」+ 完成 toast | ✅ |
| UX-07 情绪搜索 + 最近使用 | 搜索框 + `recentEmos` 置顶 | ✅ 复测确认 |
| UX-08 大纲树命中高亮 | 树视图 `<mark>` | ✅ |
| UX-09 AI 历史筛选 + 清空确认 | 类型下拉 + 关键词 + confirm | ✅ |
| UX-10 设置保存反馈 | 保存 toast + 未保存确认 | ✅ |
| UX-11 素材台默认收起 | 首次收起 + 记忆 | ✅ |
| UX-13 toast 队列 | 逐条足时展示 | ✅ |
| UX-14 AI 删除防误触 | confirm | ✅ |
| UX-15 下载 toast | 统一「已导出」 | ✅ |
| UX-16 空槽提示去重 | 初始文案缩短 | ✅ 复测确认 |

---

## 二、本次复测新观察的体验点

### N-01 [严重] 换说法快捷键失效，作者会以为「选中没反应」
- 复测确凿：`Ctrl+Shift+Enter` 实际触发的是补全（`BUG_REPORT.md` BUG-001）。作者选中文字按快捷键想换说法，弹出的是补全候选，直观感受是「快捷键对不上」「点了没反应」。
- 影响：换说法只能靠选区工具条按钮，快捷键形同虚设。
- 建议：修 `editor.js:881` 加 `!e.shiftKey`；同时建议给快捷键做**功能映射自检**（Alt+P/E/R、Ctrl+Enter、Ctrl+Shift+Enter 各触发什么，统一走一个分发器）。

### N-02 换说法弹窗候选与补全弹窗视觉难区分
- 复测观察：换说法（本地情绪词替换）和补全（词库大集合）都用同一个 `#suggest-popup`，外观几乎一致，只有 badge 不同。作者可能混淆「这次弹的是替换还是补全」。
- 建议：换说法弹窗加标题行「替换「XXX」为：」+ 更明显的替换动作按钮（回车即替换，Tab 仅预览）。

### N-03 情绪选择器「最近使用」分组在列表中间，视觉上不够突出
- 复测观察：`populateEmotions` 把「最近使用」放在搜索框后、「全部情绪」分组前，但分组标题样式较淡，作者可能没注意到置顶项。
- 建议：最近使用分组加背景高亮或「最近」徽标。

### N-04 流式输出「逐字出现」在快速文本下感知不强
- 复测观察：DeepSeek 流式逐字（已确认 `onDelta` 生效），但短句 2-3 秒就写完，作者几乎感觉不到「流式」差异。
- 建议：可选——流式时在结果区给一个「逐字输出中…」的可见光标/进度动画，强化「正在生成」的感知；长文本场景才真正体现流式价值。

### N-05 反AI 问题面板问题较多时缺「一键处理」入口
- 复测观察：T1 禁用词有「替换」按钮（v2.9 已做），但 T2/T3/句式类问题只有「跳转定位」，需逐条手动改。
- 建议：问题面板加「全部替换同类词」或按规则批量处理（如 T2 同段聚集的某一词一次替换）。

### N-06 对比区差异行没有「复制该处差异」快捷操作
- 复测观察：对比区差异（`m-del/m-ins`）可 hover 高亮、可跳转，但不能快速把「新版这句话」复制到定稿区。
- 建议：差异行 hover 加「采纳此句到定稿」按钮（与 UX 建议的「采纳差异」呼应）。

---

## 三、建议添加的便利功能（按价值排序）

### F-01 差异一键「采纳到定稿」⭐ 最推荐
- 场景：作者并排对比 2-4 份草稿，选定某版某段更优，需手动复制到下方定稿区。
- 建议：对比区每段（或每处差异）hover 出「采纳此段 → 定稿区」按钮，直接替换定稿区对应位置或追加到光标处；可配合「采纳整栏」。
- 价值：对比→定稿是核心工作流，当前断在两处，打通后效率提升最大。

### F-02 AI 出稿「批量插入」
- 场景：AI 面板一次产出 3 个版本，作者常想「都看看再挑」，或整段替换。
- 建议：结果卡加「全部插入到文末」「逐版本追加」按钮；或拖拽排序后一键插入。

### F-03 快捷键总览面板（`?` 弹出）
- 场景：作者记不住 Ctrl+Enter/Ctrl+Shift+Enter/Alt+P/E/R 等。
- 建议：按 `?`（或帮助入口）弹出快捷键速查表；并修好当前失效的 Ctrl+Shift+Enter（N-01）。
- **✅ 已实现并升级为「用户自定义快捷键」（v2.15.0）**：`?` 弹出速查面板（`#shortcut-modal`）；设置新增「快捷键」tab，8 个动作可自定义组合键/禁用/冲突提示/恢复默认（`js/hotkeys.js` + 即时保存）。

### F-04 词库「收藏夹 / 星标」
- 场景：词库 6600+ 条，常用表达每次要搜或靠情绪候选。
- 建议：词条可点星收藏，收藏的置顶参与补全/素材台；独立于情绪和分类。

### F-05 反AI 报告导出
- 场景：作者想把「本次正文的问题清单」给编辑/自己留档。
- 建议：反AI 问题面板加「导出报告 md」（问题 + 位置 + 建议替换），与差异报告并列。

### F-06 大纲章节「写作进度」标记
- 场景：长卷大纲，作者需要知道哪些章已写、哪些待写。
- 建议：大纲节点可标记「已写/写中/待写」，与定稿区联动（送定稿自动标「已写」）。

### F-07 备份「版本化」
- 场景：备份 zip 每次同名覆盖，无法回溯历史。
- 建议：备份文件名带时间戳，或保留最近 N 份历史备份。

### F-08 草稿自动保存 + 崩溃恢复
- 场景：定稿区 textarea 内容刷新后丢失（复测确认：定稿区内容不持久化）。
- 建议：定稿区内容自动保存（如每 30s + 失焦），刷新/重开恢复；对比区已持久化，定稿区也应一致。

---

## 四、做得好的体验（建议保持）

- **完全离线可用**：对比/定稿/反AI/词库开箱即用，无联网门槛。
- **反AI 实时检查**：输入即扫描、问题可点击跳转并自动选中、错误/警告可筛选。
- **@ 词库搜索**：候选带分类徽标与释义，键盘流顺畅。
- **AI 注入透明**：结果卡可见「用了哪些素材/命中哪几段前文/本次关键词」，可删词重跑。
- **选区即入口**：选字即弹 润色/扩写/重写/换说法 工具条。
- **前文库**：段落级搜索带高亮定位，章节可停用/钉住/排除。
- **大纲↔定稿联动**：送定稿/加前文库一键贯通。
- **流式输出已真实生效**：v2.13.1 修复后逐字流式 + 响应为真 SSE（复测确认）。

---

## 五、优化点优先级

1. **P0**：修 `BUG_REPORT.md` BUG-001（Ctrl+Shift+Enter 换说法失效）——**✅ 已修（v2.14.0）**，`editor.js` 补 `!e.shiftKey` + `#btn-replace` 补 `stopPropagation`。
2. **P1（写作主流程）**：F-01 差异采纳到定稿 → F-08 定稿区自动保存 → F-02 AI 批量插入 —— **✅ 已全部实现（v2.14.0）**。
3. **P2（效率）**：F-03 快捷键总览 → F-04 词库收藏 → N-02 换说法弹窗区分 → N-05 反AI 批量处理 —— **✅ 已全部实现（v2.14.0）**。
4. **P3（细节）**：N-03 最近使用分组突出 → N-04 流式感知 → N-06 差异行快捷操作 → F-05/06/07 —— **✅ 已全部实现（v2.14.0）**。

> 说明：F-01~F-08 为「新增功能」建议（非缺陷），供产品决策；N-01~N-06 为本次复测的体验观察。**v2.14.0 已按此优先级全部落地**，具体实现见 `TESTING.md` 第 9 节 v2.14.0。
